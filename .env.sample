# Copy to .env and fill in a generated value: openssl rand -hex 24
# Auth for the db (valkey) and arc (kvrocks) stores — see compose.yml and
# config.ts. Without this, both are unauthenticated Redis-protocol servers
# holding every user's keys and NWC connections.
DB_PASSWORD=
